platform :osx, '10.11'

target 'testUI1' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for testUI1
  pod 'CustomSegue', :git => 'https://github.com/DimaRU/CustomSegue.git'
  #pod 'CustomSegue', :path => '../../CustomSegue/CustomSegue.podspec'
end
